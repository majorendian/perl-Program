language: perl
perl:
  "5.32"

before_deploy:
    "cd tools; bash create-module-package.sh; cd .."
deploy:
  provider: releases
  file: dist/perl-Program.tar.gz
  skip-cleanup: true
  on:
    tags: true
  prerelease: true